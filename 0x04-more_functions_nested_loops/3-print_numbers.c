#include "main.h"
/**
 * print_numbers - A function that prints the numbers form 0 to 9.
 *
 * Return: Always 0.
 */
void print_numbers(void)
{
	int a;

	for (a = 0; a < 10; ++a)
	{
		_putchar(a);
	}
	_putchar('\n');
}
